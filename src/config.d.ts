import { IConfig } from "./types";

export const config: IConfig = {
  maxPostsQuantity: 10,
  maxNavBarPageButtonsCount: 5,
  defaultSortKey: 'title',
  defaultNavigationMin: 0,
  defaultNavigationMax: 5,
}

